<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
    xmlns:local="using:mvvm.tests"

    x:Class="mvvm.tests.MyEntityView"
    d:DesignWidth="510" d:DesignHeight="320"
    >
    <UserControl.Resources>
        <x:Int32 x:Key="Int43">43</x:Int32>
        <local:StringToInt32ValueConverter x:Key="StringToInt32ValueConverter" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBox
            x:Name="TextBox"
            Grid.Row="0" Grid.Column="0"
            Margin="10" Width="90"
            Text="{x:Bind ViewModel.MyProperty,Mode=TwoWay}"
            Header="MyProperty"
            />
        <StackPanel
            Grid.Row="0" Grid.Column="1"
            Margin="10,0,10,0"
            >
            <CheckBox
                Margin="0,5,0,0"
                Content="Allow Increment"
                IsChecked="{x:Bind ViewModel.IsIncrementAvailable,Mode=TwoWay}"
                />

            <Button
                Margin="27,-4,0,0" Width="101"
                Command="{x:Bind ViewModel.IncrementCommand,Mode=OneTime}"
                Content="Increment"
                />
        </StackPanel>
        <StackPanel
            Grid.Row="0" Grid.Column="2"
            Margin="10,0,10,0"
            >
            <CheckBox
                Margin="0,5,0,0"
                Content="Allow Decrement"
                IsChecked="{x:Bind ViewModel.IsDecrementAvailable,Mode=TwoWay}"
                />

            <Button
                Margin="27,-4,0,0" Width="101"
                Command="{x:Bind ViewModel.DecrementCommand,Mode=OneTime}"
                Content="Decrement"
                />
        </StackPanel>

        <Button
            Grid.Row="2" Grid.Column="1"
            Margin="37,10,10,10" Width="101"
            Content="String"
            Command="{x:Bind ViewModel.StringCommand}"
            CommandParameter="Hello, there!"
            />

        <Button
            Grid.Row="2" Grid.Column="2"
            Margin="37,10,10,10" Width="101"
            Content="Int32"
            Command="{x:Bind ViewModel.Int32Command}"
            CommandParameter="{x:Bind TextBox.Text,Mode=OneWay,Converter={StaticResource StringToInt32ValueConverter}}"
            />
            <!-- Keeping these around for future reference. -->
            <!--CommandParameter="{x:Bind ViewModel.MyProperty,Mode=OneWay}"-->
            <!--CommandParameter="{StaticResource Int43}"-->
            <!--CommandParameter="{x:Null}"-->
    </Grid>
</UserControl>
