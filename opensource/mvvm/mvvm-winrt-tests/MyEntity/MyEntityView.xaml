<UserControl
    x:Class="mvvm.tests.MyEntityView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock
            Grid.Row="0" Grid.Column="0"
            Margin="10" Width="90"
            Text="{x:Bind ViewModel.MyProperty,Mode=OneWay}"
            />
        <Button
            Grid.Row="0" Grid.Column="1"
            x:Name="IncrementButton"
            Margin="10" Width="90"
            Command="{x:Bind ViewModel.IncrementCommand}"
            Content="Increment" />
        <Button
            Grid.Row="0" Grid.Column="2"
            x:Name="DecrementButton"
            Margin="10" Width="90"
            Command="{x:Bind ViewModel.DecrementCommand}"
            Content="Decrement"
            />

        <CheckBox
            Grid.Row="1" Grid.ColumnSpan="3"
            Margin="10"
            Content="Increment is available"
            IsChecked="{x:Bind ViewModel.IsIncrementAvailable,Mode=TwoWay}"
            />

        <CheckBox
            Grid.Row="2" Grid.ColumnSpan="3"
            Margin="10"
            Content="Decrement is available"
            IsChecked="{x:Bind ViewModel.IsDecrementAvailable,Mode=TwoWay}"
            />
    </Grid>
</UserControl>
